services:
  new-api:
    # This override is for GHCR deployments, not the US local-build blue-green flow.
    # Replace this placeholder with the digest emitted by dev-image.yml.
    image: ghcr.io/your-owner/new-api@sha256:replace-with-published-digest
